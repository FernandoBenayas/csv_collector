#CSV COLLECTOR

Just build it and run it, and it will detect all the indices, collect all the documents from each index and turn them into csv. 


Be advised: it will take a lot of time with a fairly big data size. 
